[package]
name = "provers"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "^3.0.5", default-features = false, features = ['std'] }
k256 = { version = "0.13.1", default-features = false, features = ["ecdsa", "schnorr"] }
sha2 = "0.10.8"
common={path="../../common"}
bytemuck = { version = "1.14", features = ["derive"] }